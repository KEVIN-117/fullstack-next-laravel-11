import { ICategory } from "../../types/category";

export function CategoryDetails({ category }: { category: ICategory }) {
    return (
        <div>
            <h1>Category Details</h1>
        </div>
    );
}